<!DOCTYPE html>
<html>
<head>

<title>PasteHub</title>

<link rel="stylesheet" href="/style.css">

</head>

<body>

<div class="container">

<h1>PasteHub</h1>

<div class="top-buttons">

<a href="/create">
<button>➕ Create New Paste</button>
</a>

<a href="/folder/add">
<button>📁 Add Folder</button>
</a>

</div>

<h2>Folders</h2>

<%
const folderMap = {};

pastes.forEach(paste => {

    const folder = paste.folder || "Uncategorized";

    if (!folderMap[folder]) {
        folderMap[folder] = [];
    }

    folderMap[folder].push(paste);

});
%>


<% if (Object.keys(folderMap).length === 0) { %>

<p>No folders yet.</p>

<% } %>


<% Object.keys(folderMap).forEach(folder => { %>

<div class="folder-box">

    <h3>
        📁
        <a href="/folder/<%= encodeURIComponent(folder) %>">
            <%= folder %>
        </a>
    </h3>

    <p>
        <%= folderMap[folder].length %>
        paste<%= folderMap[folder].length === 1 ? "" : "s" %>
    </p>

</div>

<br>

<% }) %>


</div>

</body>
</html>
