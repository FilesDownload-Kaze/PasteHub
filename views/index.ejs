<!DOCTYPE html>
<html>
<head>

<title>PasteHub</title>

<link rel="stylesheet" href="/style.css">

<meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>

<body>

<div class="container">

    <!-- HEADER -->
    <!-- TOP NAVBAR -->
<header class="top-navbar">

    <div class="nav-left">
        <span class="nav-logo">📋</span>
        <span class="nav-title">PasteHub</span>
    </div>

    <div class="nav-right">

        <button
            class="menu-button"
            onclick="toggleTopMenu(event)">
            ⋮
        </button>

        <div id="topMenu" class="menu-dropdown">

            <a href="#">
                👤 Account
            </a>

            <a
                href="https://t.me/KAZEHAYAMODZ"
                target="_blank">
                💬 Contact Developer
            </a>

            <a href="/logout">
                🚪 Logout
            </a>

        </div>

    </div>

</header>

<!-- HERO -->
<div class="hero">

    <h1>Welcome back 👋</h1>

    <p>
        Manage all your pastes in one place.
    </p>

</div>


    <!-- QUICK ACTIONS -->
    <div class="quick-actions">

        <a href="/create" class="action-card">
            <span class="action-icon">＋</span>

            <div>
                <strong>New Paste</strong>
                <small>Create a new paste</small>
            </div>
        </a>


        <a href="/folder/add" class="action-card">
            <span class="action-icon">📁</span>

            <div>
                <strong>New Folder</strong>
                <small>Organize your pastes</small>
            </div>
        </a>

    </div>


    <!-- FOLDERS -->
    <section class="home-section">

        <div class="section-header">

            <div>
                <h2>📁 Folders</h2>

                <p>
                    Organize your pastes
                </p>
            </div>

        </div>


        <% if (!folders || folders.length === 0) { %>

            <div class="empty-state">

                <div class="empty-icon">📁</div>

                <strong>No folders yet</strong>

                <p>
                    Create your first folder.
                </p>

            </div>

        <% } else { %>


            <div class="folder-list">

                <% folders.forEach(folder => { %>

                    <%
                    const folderPastes = (pastes || []).filter(
                        paste => paste.folder === folder.name
                    );
                    %>


                    <div class="folder-card">

                        <a
                            class="folder-main"
                            href="/folder/<%= encodeURIComponent(folder.name) %>"
                        >

                            <div class="folder-icon">
                                📁
                            </div>

                            <div class="folder-info">

                                <strong>
                                    <%= folder.name %>
                                </strong>

                                <span>
                                    <%= folderPastes.length %>
                                    paste<%= folderPastes.length === 1 ? "" : "s" %>
                                </span>

                            </div>

                            <span class="folder-arrow">
                                →
                            </span>

                        </a>


                        <!-- MENU -->
                        <div class="card-menu">

                            <button
                                type="button"
                                class="menu-button"
                                onclick="toggleMenu(this)"
                            >
                                ⋮
                            </button>


                            <div class="menu-dropdown">

                                <a
                                    href="/folder/rename/<%= encodeURIComponent(folder.name) %>"
                                >
                                    ✏️ Rename
                                </a>

                            </div>

                        </div>

                    </div>

                <% }) %>

            </div>

        <% } %>

    </section>


    <!-- OTHER PASTES -->
    <section class="home-section">

        <div class="section-header">

            <div>
                <h2>📦 Other Pastes</h2>

                <p>
                    Pastes outside folders
                </p>
            </div>

        </div>


        <%
        const noFolderPastes = (pastes || []).filter(
            paste => !paste.folder || paste.folder === "None"
        );
        %>


        <% if (noFolderPastes.length === 0) { %>

            <div class="empty-state">

                <div class="empty-icon">
                    📄
                </div>

                <strong>
                    No standalone pastes
                </strong>

                <p>
                    Create a paste to get started.
                </p>

            </div>


        <% } else { %>


            <div class="paste-list">

                <% noFolderPastes.forEach(paste => { %>

                    <div class="paste-card">

                        <a
                            href="/paste/<%= paste.id %>"
                            class="paste-main"
                        >

                            <strong class="paste-title">
                                <%= paste.title %>
                            </strong>


                            <div class="paste-meta">

                                <span>
                                    👁️ <%= paste.views %> views
                                </span>

                                <span>
                                    📅
                                    <%= new Date(paste.created).toLocaleString() %>
                                </span>

                            </div>

                        </a>


                        <!-- MENU -->
                        <div class="card-menu">

                            <button
                                type="button"
                                class="menu-button"
                                onclick="toggleMenu(this)"
                            >
                                ⋮
                            </button>


                            <div class="menu-dropdown">

                                <a
                                    href="/edit/<%= paste.id %>?key=<%= paste.editKey %>"
                                >
                                    ✏️ Edit
                                </a>

                                <a
                                    href="/paste/move/<%= paste.id %>"
                                >
                                    📁 Move
                                </a>

                            </div>

                        </div>

                    </div>

                <% }) %>

            </div>

        <% } %>

    </section>


</div>


<script>

function toggleTopMenu(event){

    event.stopPropagation();

    // Isara muna lahat ng card menus
    document.querySelectorAll(".card-menu .menu-dropdown").forEach(function(menu){
        menu.classList.remove("show");
    });

    document
        .getElementById("topMenu")
        .classList.toggle("show");

}

function toggleMenu(button) {

    document
        .getElementById("topMenu")
        ?.classList.remove("show");

    const menu = button.nextElementSibling;

    document.querySelectorAll(".card-menu .menu-dropdown").forEach(function(item){

        if(item !== menu){
            item.classList.remove("show");
        }

    });

    menu.classList.toggle("show");

}

document.addEventListener("click", function(event) {

    document
        .getElementById("topMenu")
        ?.classList.remove("show");

    if (!event.target.closest(".card-menu")) {

        document.querySelectorAll(".menu-dropdown").forEach(function(menu) {

            menu.classList.remove("show");

        });

    }

});

</script>

</body>
</html>
